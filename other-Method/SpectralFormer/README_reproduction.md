# SpectralFormer Reproduction

This folder contains a runnable PyTorch reproduction entry for SpectralFormer. The implementation in `../common/model_zoo.py` follows the group-wise spectral embedding and transformer encoder family from the official repository: https://github.com/danfenghong/IEEE_TGRS_SpectralFormer.

Run this method alone with `run_reproduction.py`.
