# SSRN Reproduction

This folder contains a runnable PyTorch reproduction entry for SSRN. The implementation in `../common/model_zoo.py` follows the spectral residual blocks plus spatial residual blocks design from the official repository: https://github.com/zilongzhong/SSRN.

Run this method alone with `run_reproduction.py`.
