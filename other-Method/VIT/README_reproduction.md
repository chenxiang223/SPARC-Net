# ViT Reproduction

This folder contains a runnable PyTorch reproduction entry for Vision Transformer. The implementation in `../common/model_zoo.py` follows the patch embedding, class token, positional embedding, and transformer encoder design from the Google Research repository: https://github.com/google-research/vision_transformer.

Run this method alone with `run_reproduction.py`.
