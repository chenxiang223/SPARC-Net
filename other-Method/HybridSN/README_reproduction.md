# HybridSN Reproduction

This folder contains a runnable PyTorch reproduction entry for HybridSN. The implementation in `../common/model_zoo.py` follows the 3-D to 2-D convolution hierarchy from the official repository: https://github.com/gokriznastic/HybridSN.

Run this method alone with `run_reproduction.py`.
